{
  "name": "LLM-Driven Optimization Loop for coint4",
  "branchName": "feature/llm-driven-optimization-loop-coint4",
  "userStories": [
    {
      "id": "US-001",
      "title": "Create standalone autonomous LLM orchestrator",
      "description": "As an optimization engineer, I want a separate LLM loop script so we can avoid regressions in the current autopilot.",
      "acceptanceCriteria": [
        "Create `coint4/scripts/optimization/autonomous_optimize_llm.py`.",
        "Do not modify `coint4/scripts/optimization/autopilot_budget1000.py`.",
        "Support tick mode and continuous mode.",
        "Allow only one active `run_group` at a time.",
        "`make lint` passes.",
        "`make test` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run --max-ticks 1` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run-mock-llm --max-ticks 1` passes."
      ],
      "priority": 1,
      "passes": false,
      "dependsOn": []
    },
    {
      "id": "US-002",
      "title": "Select clean candidates from rollup",
      "description": "As an analyst, I want strict clean filtering so the LLM makes decisions only from valid results.",
      "acceptanceCriteria": [
        "Clean filter includes completed run, valid canonical metrics, and passed risk gates.",
        "LLM candidates are top-20 globally plus top-5 from the latest `run_group` with deduplication.",
        "Build a compact summary for the LLM with metrics and plateau signals.",
        "If no clean candidates exist, log an event and enter retry/backoff.",
        "`make lint` passes.",
        "`make test` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run --max-ticks 1` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run-mock-llm --max-ticks 1` passes."
      ],
      "priority": 2,
      "passes": false,
      "dependsOn": [
        "US-001"
      ]
    },
    {
      "id": "US-003",
      "title": "Implement LLM decider via codex exec",
      "description": "As a strategy owner, I want the next-batch or stop decision to be made only by LLM via `codex exec`.",
      "acceptanceCriteria": [
        "Invoke LLM only with subprocess command `codex exec`.",
        "Validate LLM response with strict JSON Schema.",
        "Reject `action=stop` before at least two completed batches.",
        "Store a human-readable decision reason in logs and decision JSON.",
        "`make lint` passes.",
        "`make test` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run --max-ticks 1` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run-mock-llm --max-ticks 1` passes."
      ],
      "priority": 3,
      "passes": false,
      "dependsOn": [
        "US-002"
      ]
    },
    {
      "id": "US-004",
      "title": "Generate batches with idempotency",
      "description": "As an operator, I want automatic next-batch generation without duplicates so the loop stays reproducible.",
      "acceptanceCriteria": [
        "Generate deterministic and unique `run_group` values.",
        "Create `run_queue.csv` in the canonical path.",
        "Enforce `walk_forward.max_steps <= 5` in all generated configs.",
        "Repeated runs do not create duplicate planned/completed equivalent profiles.",
        "`make lint` passes.",
        "`make test` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run --max-ticks 1` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run-mock-llm --max-ticks 1` passes."
      ],
      "priority": 4,
      "passes": false,
      "dependsOn": [
        "US-003"
      ]
    },
    {
      "id": "US-005",
      "title": "Run heavy execution only on VPS",
      "description": "As an infrastructure operator, I want heavy runs to execute only via the remote helper on the target VPS.",
      "acceptanceCriteria": [
        "Use `coint4/scripts/remote/run_server_job.sh` for heavy runs.",
        "Default target IP is `85.198.90.128`.",
        "Keep auto shutdown behavior with `STOP_AFTER=1` by default.",
        "Do not run heavy WFA workloads on the local server.",
        "`make lint` passes.",
        "`make test` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run --max-ticks 1` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run-mock-llm --max-ticks 1` passes."
      ],
      "priority": 5,
      "passes": false,
      "dependsOn": [
        "US-004"
      ]
    },
    {
      "id": "US-006",
      "title": "Add resilience and recovery",
      "description": "As an operator, I want the loop to survive external dependency failures without crashing.",
      "acceptanceCriteria": [
        "Implement backoff `1m->2m->5m->10m->20m->30m` with jitter for Codex, Serverspace, and SSH failures.",
        "Record all failures as loop events.",
        "After restart, continue from persisted state.",
        "Detect stale lock by TTL and support safe lock reclaim.",
        "`make lint` passes.",
        "`make test` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run --max-ticks 1` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run-mock-llm --max-ticks 1` passes."
      ],
      "priority": 6,
      "passes": false,
      "dependsOn": [
        "US-003",
        "US-005"
      ]
    },
    {
      "id": "US-007",
      "title": "Implement observability and stop reporting",
      "description": "As an autopilot user, I want transparent LLM decisions and a final stop report.",
      "acceptanceCriteria": [
        "Save each LLM decision to `docs/autopilot_llm/decisions/decision_<timestamp>.json`.",
        "Write per-tick logs to `docs/autopilot_llm/loop.log`.",
        "On STOP, create `docs/autopilot_llm/final_report_<timestamp>.md`.",
        "Final report includes `best_run_id`, artifact paths, and stop reason.",
        "`make lint` passes.",
        "`make test` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run --max-ticks 1` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run-mock-llm --max-ticks 1` passes."
      ],
      "priority": 7,
      "passes": false,
      "dependsOn": [
        "US-003",
        "US-005"
      ]
    },
    {
      "id": "US-008",
      "title": "Support dry-run modes",
      "description": "As a developer, I want dry-run modes with real or mock LLM so I can validate the orchestration quickly.",
      "acceptanceCriteria": [
        "`--dry-run` uses real `codex exec` but does not trigger VPS execution.",
        "`--dry-run-mock-llm` does not call Codex and uses fixture/mock decisions.",
        "Both modes write state, decision, and logs in the same formats as real mode.",
        "Dry-run results are clearly marked as simulation.",
        "`make lint` passes.",
        "`make test` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run --max-ticks 1` passes.",
        "`cd coint4 && PYTHONPATH=src ./.venv/bin/python scripts/optimization/autonomous_optimize_llm.py --dry-run-mock-llm --max-ticks 1` passes."
      ],
      "priority": 8,
      "passes": false,
      "dependsOn": [
        "US-006",
        "US-007"
      ]
    }
  ],
  "metadata": {
    "updatedAt": "2026-02-19T20:18:49.312Z"
  }
}