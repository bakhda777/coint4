# Best Practice Optuna Optimization

Реализация правильной оптимизации параметров pairs trading с использованием best practices.

## 🎯 Ключевые улучшения

### ✅ Правильная логика zscore параметров
- **Гистерезис**: `zscore_exit = zscore_threshold - hysteresis`
- **Валидация**: обязательный gap >= 0.7 между входом и выходом
- **Ограничения**: zscore_threshold >= 1.6, zscore_exit <= 0.6

### ✅ Анти-чурн штрафы
- Штраф за частые сделки: `penalty = 0.02 * max(0, trades_per_day - 5)`
- Предотвращает переоптимизацию на шуме

### ✅ Правильные настройки Optuna
- **TPE sampler** с seed=42 и n_startup_trials=50
- **MedianPruner** с n_warmup_steps=30
- **SQLite storage** для сохранения прогресса

### ✅ Критерии качества
- In-sample Sharpe >= 1.3
- Out-of-sample Sharpe >= 1.0
- Max drawdown <= 25%
- Trades per day <= 5
- Win rate >= 55%

## 📁 Структура файлов

```
configs/
├── search_space_bp_balanced.yaml     # Сбалансированное пространство поиска
├── search_space_bp_conservative.yaml # Консервативное пространство
└── search_space_refine.yaml          # Для рефайна (создается автоматически)

scripts/
├── bp_optimize.py                    # Основной скрипт оптимизации
└── bp_validate.py                    # Скрипт валидации результатов
```

## 🚀 Быстрый старт

### 1. Balanced оптимизация (400 trials)

```bash
python scripts/bp_optimize.py \
  --base configs/main_2024.yaml \
  --space configs/search_space_bp_balanced.yaml \
  --trials 400 \
  --study wf_best_practice_balanced \
  --storage sqlite:///studies.db \
  --n-jobs 1
```

### 2. Валидация результатов

```bash
python scripts/bp_validate.py \
  --study wf_best_practice_balanced \
  --storage sqlite:///studies.db
```

### 3. Conservative оптимизация (при высоких комиссиях)

```bash
python scripts/bp_optimize.py \
  --base configs/main_2024.yaml \
  --space configs/search_space_bp_conservative.yaml \
  --trials 300 \
  --study wf_best_practice_cons \
  --storage sqlite:///studies.db
```

## 📊 Пространства поиска

### Balanced (рекомендуемое)
- `zscore_threshold`: 1.6 - 2.3
- `hysteresis`: 0.7 - 1.1
- `rolling_window`: 20 - 40 (step=5)
- `max_active_positions`: 10 - 20 (step=5)
- `risk_per_position_pct`: 0.008 - 0.020
- Анти-чурн штраф: 0.02

### Conservative (для высоких комиссий)
- `zscore_threshold`: 1.8 - 2.8
- `hysteresis`: 0.8 - 1.4
- `rolling_window`: 30 - 60 (step=5)
- Анти-чурн штраф: 0.03

## 🔧 Настройки воспроизводимости

Все скрипты используют фиксированные seeds:
- `random.seed(42)`
- `np.random.seed(42)`
- `optuna.samplers.TPESampler(seed=42)`

## 📈 Процесс оптимизации

### Этап 1: Balanced оптимизация
1. Запуск 400 trials на сбалансированном пространстве
2. Валидация лучших параметров
3. Проверка критериев качества

### Этап 2: Conservative (при необходимости)
1. Если результаты "шумные" или комиссии высокие
2. Запуск 300 trials на консервативном пространстве
3. Валидация результатов

### Этап 3: Refine (опционально)
1. Создание узкого пространства вокруг лучшего решения
2. Запуск 200 trials для финальной настройки
3. Итоговая валидация

## 🎯 Критерии принятия решения

Параметры принимаются если:
- ✅ In-sample Sharpe >= 1.3
- ✅ Out-of-sample Sharpe >= 1.0
- ✅ Max drawdown <= 25%
- ✅ Trades per day <= 5
- ✅ Win rate >= 55%
- ✅ zscore_threshold >= 1.6
- ✅ zscore_exit <= 0.6
- ✅ gap (threshold - exit) >= 0.7

Если критерии не выполняются:
- Увеличить hysteresis нижнюю границу на 0.1
- Повторить оптимизацию

## 📋 Логи и артефакты

### Создаются автоматически:
- `logs/{study_name}_bp_optimization.log` - детальные логи
- `best_params_{study_name}.json` - лучшие параметры
- `validation_{study_name}_{timestamp}.json` - результаты валидации
- `studies.db` - SQLite база с историей оптимизации

### Структура лога:
```
--- TRIAL 1 ---
ПАРАМЕТРЫ TRIAL:
  zscore_threshold: 2.1234
  zscore_exit: 1.4234
  hysteresis: 0.7000
  ...
РЕЗУЛЬТАТ TRIAL:
  Базовая метрика: 1.2345
  Анти-чурн штраф: 0.0200
  Финальное значение: 1.2145
```

## 🔍 Анализ результатов

### Проверка zscore логики:
```python
zscore_threshold = 2.1
hysteresis = 0.8
zscore_exit = zscore_threshold - hysteresis  # = 1.3
gap = zscore_threshold - zscore_exit  # = 0.8 (должно быть >= 0.7)
```

### Анти-чурн штраф:
```python
trades_per_day = 6.5
max_allowed = 5.0
penalty = 0.02 * max(0, 6.5 - 5.0)  # = 0.03
final_score = base_score - penalty
```

## ⚠️ Важные замечания

1. **Не редактировать zscore_exit напрямую** - он вычисляется автоматически
2. **Обязательная валидация** после каждой оптимизации
3. **Проверка критериев** перед принятием параметров
4. **Сохранение артефактов** для воспроизводимости
5. **Использование SQLite storage** для продолжения прерванных прогонов

## 🚨 Troubleshooting

### Если много pruned trials:
- Ослабить ограничения в validation секции конфига
- Проверить min_zscore_gap (возможно слишком строгий)

### Если нет улучшения после 100+ trials:
- Переключиться на conservative пространство
- Проверить качество данных
- Увеличить n_startup_trials

### Если результаты нестабильны:
- Увеличить hysteresis диапазон
- Добавить больше анти-чурн штрафов
- Проверить комиссии и slippage
