# 📊 Outputs Directory

Директория для хранения результатов оптимизации, анализа и бэктестов.

## 📁 Структура

### studies/
База данных Optuna с результатами оптимизации.

- **Формат файлов**: `{study_name}_{timestamp}.db`
- **Хранение**: Последние 10 актуальных баз
- **Архив**: Старые базы перемещаются в `archive/old_studies/`

### Примеры баз:
- `clean_sharpe_*.db` - Результаты чистой оптимизации Sharpe
- `iterative_*.db` - Результаты итеративной оптимизации
- `fast_parallel_*.db` - Результаты параллельной оптимизации

### preselected_pairs.csv
Предварительно отобранные пары для торговли.

**Структура:**
- `symbol1, symbol2` - Торговая пара
- `pvalue` - P-value коинтеграции
- `half_life` - Half-life в барах
- `spread_std` - Стандартное отклонение спреда

## 🔧 Работа с базами Optuna

### Загрузка исследования:
```python
import optuna

study = optuna.load_study(
    study_name="clean_sharpe_20240810",
    storage="sqlite:///outputs/studies/clean_sharpe_20240810.db"
)
```

### Анализ результатов:
```python
# Лучшие параметры
print(study.best_params)
print(f"Best Sharpe: {study.best_value:.3f}")

# Визуализация
optuna.visualization.plot_optimization_history(study)
optuna.visualization.plot_param_importances(study)
```

### Очистка старых баз:
```bash
# Архивировать базы старше 30 дней
find outputs/studies -name "*.db" -mtime +30 -exec mv {} archive/old_studies/ \;
```

## 📈 Метрики в базах

Каждый trial содержит:
- **value**: Sharpe ratio (целевая метрика)
- **params**: Оптимизируемые параметры
- **user_attrs**: Дополнительные метрики
  - `total_trades` - Количество сделок
  - `win_rate` - Процент прибыльных сделок
  - `max_drawdown` - Максимальная просадка
  - `total_pnl` - Общий PnL

## 🗄️ Управление размером

- Регулярно архивируйте старые базы
- Используйте `optuna.delete_study()` для удаления ненужных исследований
- Сжимайте архивные базы: `gzip archive/old_studies/*.db`