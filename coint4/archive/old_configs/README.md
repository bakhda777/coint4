# ⚙️ Configurations Directory

Структурированная организация конфигурационных файлов.

## 📁 Структура

### base/
Основные конфигурации системы.

- **main.yaml** - Основная конфигурация (production)
  - Параметры данных и нормализации
  - Настройки портфеля
  - Фильтры отбора пар
  - Walk-forward параметры
  - Издержки торговли

- **main_improved.yaml** - Улучшенная конфигурация
  - Более строгие фильтры пар
  - Оптимизированные параметры портфеля
  - Улучшенный risk management

### search_spaces/
Search space конфигурации для Optuna оптимизации.

- **optimized.yaml** - Оптимальный search space
  - Гарантированный hysteresis между entry/exit
  - Сбалансированные диапазоны параметров
  
- **extended.yaml** - Расширенный search space
  - Широкие диапазоны для exploration
  - Дополнительные адаптивные параметры
  
- **fast.yaml** - Быстрый search space
  - Узкие диапазоны для быстрой оптимизации
  - Меньше параметров

### best_params/
Сохраненные лучшие параметры после оптимизации.

### archive/
Устаревшие и экспериментальные конфигурации.

## 🔧 Использование

### Для оптимизации:
```python
from src.optimiser.clean_objective import CleanObjective

objective = CleanObjective(
    base_config_path="configs/base/main.yaml",
    search_space_path="configs/search_spaces/optimized.yaml"
)
```

### Для бэктеста:
```python
from src.coint2.utils.config import load_config

config = load_config("configs/base/main.yaml")
```

## 📊 Ключевые параметры

### Нормализация данных
- `normalization_method: "rolling_zscore"` - Production-совместимый метод

### Фильтры пар
- `coint_pvalue_threshold: 0.1` - Порог p-value для коинтеграции
- `min_half_life_days: 0.1` - Минимальный half-life (2.4 часа)
- `max_half_life_days: 10` - Максимальный half-life (10 дней)

### Walk-forward
- `train_days: 60` - Период обучения
- `test_days: 30` - Период тестирования
- `gap_minutes: 15` - Минимальный gap (1 бар)

### Издержки
- `commission_pct: 0.0004` - Комиссия 0.04%
- `slippage_pct: 0.0005` - Проскальзывание 0.05%

## ⚠️ Важные замечания

1. **Всегда используйте `rolling_zscore`** для production
2. **Не изменяйте `gap_minutes`** - должен быть 15 (1 бар)
3. **Проверяйте hysteresis** между zscore_threshold и zscore_exit