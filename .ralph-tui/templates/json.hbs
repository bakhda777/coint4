## Task: {{taskTitle}}

{{taskDescription}}

## Rules
- Work autonomously. If something is ambiguous, pick the safest default and record the decision in `.ralph-tui/progress.md`.
- Keep diffs small and focused.
- Do not run `git add`, `git commit`, `git restore --staged`, or any other git staging/commit operations in this session.
- Never print secrets (Serverspace API key must live only in `.secrets/serverspace_api_key` and/or `SERVSPACE_API_KEY`).
- After finishing: update `.ralph-tui/progress.md` (max ~10 bullets, no long text) for short cross-task context.
  IMPORTANT: `.ralph-tui/progress.md` is intentionally gitignored. Do NOT change `.gitignore` to track it and do NOT stage/commit it.
- When the task is fully done, end your final message with exactly this marker on its own line:
  `<promise>COMPLETE</promise>`
