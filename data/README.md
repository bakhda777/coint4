# Data directory

This folder is intentionally excluded from Git.
Put large datasets here (or symlink this path), and keep only small samples in a separate tracked location if needed.

See docs/data_storage.md for storage and Git LFS guidance.
