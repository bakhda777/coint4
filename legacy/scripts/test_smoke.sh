#!/bin/bash
# Запускает только самые быстрые и критичные smoke тесты.
# Время выполнения: < 10 секунд.
set -e
echo "💨 Запуск SMOKE тестов..."
cd "$(dirname "$0")/.."
pytest -m smoke
