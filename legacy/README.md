> NOTE: Archived/legacy document. It may describe historical behavior and can be out of date. See `docs/` for current usage.

# Legacy Workspace

This directory contains the archived root-level project (old `src/`, `tests/`, `configs/`, `scripts/`, and build files)
kept for reference. The active workspace is `coint4/`.
